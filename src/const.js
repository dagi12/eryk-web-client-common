export const UPPER_CHAR_REGEXP = new RegExp('[A-Z]');
export const LOWER_CHAR_REGEXP = new RegExp('[a-z]');
export const SPECIAL_CHAR_REGEXP = /^(?=.*[$@$!%*?&#])/i;
export const DIGIT_REGEXP = /^(?=.*\d)/i;
export const PASSWORD_LENGTH = 8;
